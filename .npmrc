# Puppeteer 浏览器下载镜像配置（国内加速）
# 使用 npmmirror（原淘宝镜像）加速下载
# 注意：此配置对 npm install puppeteer 时下载 Chromium 有效
# 对于 npx puppeteer browsers install chrome，请使用环境变量或 install-browser:cn 脚本
puppeteer_download_host=https://registry.npmmirror.com/-/binary/chromium-browser-snapshots
